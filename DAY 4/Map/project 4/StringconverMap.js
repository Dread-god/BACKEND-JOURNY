const names = [
     "vedant",
     "javascript",
     "backend",
     "developer",
     "matrix",
     "resurgence"
];

const toCap = names.map((name) => { return name.toUpperCase() })

console.log(toCap);
